apiVersion: v1
kind: Pod
metadata:
  name: alpine-2
  labels:
    app: app1
spec:
  containers:
    - name: alpine2
      image: alpine:latest
      command: ["sleep", "infinity"]

    resources:
      requests: 
        memory: "512MiB"
        cpu: 1
      limits: 
        memory: "1Gi"
        cpu: 2
